@model IEnumerable<WHF.Models.PlayerSkill>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Add New", "Add", new { id = ViewBag.PlayerID })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Player.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Skill.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Attribute)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Player.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Skill.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Attribute)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.PlayerSkillID }) |*@
            @Html.ActionLink("Details", "Details", new { id=item.PlayerSkillID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PlayerSkillID })
        </td>
    </tr>
}

</table>
