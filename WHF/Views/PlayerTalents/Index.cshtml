@model IEnumerable<WHF.Models.PlayerTalent>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Add New", "Add", new { id = ViewBag.PlayerID })
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Player.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Talent.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Player.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Talent.Name)
        </td>
        <td>
            @*@Html.ActionLink("Edit", "Edit", new { id=item.PlayerTalentID }) |*@
            @Html.ActionLink("Details", "Details", new { id=item.PlayerTalentID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PlayerTalentID })
        </td>
    </tr>
}

</table>
